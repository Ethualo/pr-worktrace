{
  "name": "worktrace-bot",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
